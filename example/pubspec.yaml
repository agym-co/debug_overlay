name: debug_overlay_example
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  supernova_flutter:
    git:
      url: https://github.com/JonasWanke/supernova.git
      path: supernova_flutter
      ref: 729c52099c0f6979af80b31b0102148cd967c8f4

dependency_overrides:
  debug_overlay:
    path: ../

flutter:
  uses-material-design: true
